<% @bargains.each do |bargain| %>
  <%= link_to bargain.product.name, bargain.product.url, target: "_blank" %></br>
  <%= "#{bargain.created_at.strftime("%Y-%m-%d %H:%M:%S")} [#{bargain.price}] [#{bargain.discount}]" %></br>
<% end %>
<%= will_paginate @bargains %>